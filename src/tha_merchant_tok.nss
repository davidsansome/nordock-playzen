#include "tha_housing_costs"

void main()
{
    SetCustomToken(602,IntToString(SMALL_HOUSE_COST));
    SetCustomToken(601,IntToString(MED_HOUSE_COST));
    SetCustomToken(600,IntToString(LARGE_HOUSE_COST));
}
